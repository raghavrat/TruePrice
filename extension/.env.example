# Base URL of the deployed marketing site (hosts the /api/ai/profile proxy).
# For local dev with the web app running on Next.js default port:
VITE_API_BASE_URL=http://localhost:3000

# Supabase project (anon key is safe to ship in the extension).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
